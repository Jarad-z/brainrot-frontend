# S6 Kickoff Prompt

> Paste the content below into a fresh Claude Code session to start S6 brainstorm.

---

继续 Brainrot 前端项目。S5 已合并到 main（PR #8）—— asset 上传、批量审批、email 邀请、ws 元信息编辑、成员管理、agent 编辑回归、agents-encoding 兼容层清理全部完成。S5 期间还修了 #24 后端尾巴（messages 读路径解码）+ 加了 ThinkingBar 按 runs 接口判 active 的兜底逻辑。

## 本地状态确认（开新分支前）

1. `cd D:\brainrot_frontend && git status` —— 工作树应当干净。在 main 上，sync 到 origin/main。如果 PR #8 还没 squash merge，等一下或者用 origin/s5-collab-and-uploads 当 base 也行（征求你判断）。

2. 检查 `D:\brainrot` 后端状态：
   - 应当在 `origin/main` = `f8bf16a` 或更新（PR #2 闭环 #19-#27 + PR #3 per-card-workdir）
   - `server.exe` 在 `D:\brainrot\bin\`，**记得起新 brainstorm session 前先 `git pull` + `go build -o bin/server.exe ./cmd/server`**
   - postgres docker：`brainrot-postgres-1` (:5433)
   - 测试账号：`qa@brainrot.local` / `qa-tester-pw-1`
   - 种子工作区：`29bbd200-1a89-46d3-bfaa-ad4a1af8b32d`

3. **跟后端确认这些 gap 排期**（影响 S6 决策）：
   - **#28** `PATCH /api/v1/artifacts/{id} { excluded }` —— S6 artifact 排除按钮的前置依赖
   - **#29** 非 owner 自离工作区 —— "离开工作区"按钮的前置依赖
   - **#30** WS `message.appended` 没解 jsonb —— 前端有 base64 兜底但希望删；不阻塞 S6 但顺手提一下
   - ws 软归档（#20 末尾延后那条）—— "归档工作区"按钮的前置依赖

## S6 候选范围

S6 = "可用度 + 完整性收尾"。S5 完成了"协作 + 上传"，S6 把"日常用起来还差点意思的细节"+"完整闭环的最后几块"收掉。

候选清单（brainstorm 时挑选 + 排优先级）：

### A. Artifact 排除按钮（消费 #28）

- 前提：后端 `PATCH /artifacts/{id} { excluded }` ready
- 任务详情右栏 `ArtifactsTab` 加"排除"按钮 → PATCH → invalidate
- "显示已排除" toggle —— 取决于后端 `GET /artifacts` 是否支持 `?include_excluded=1` 参数；如果不支持，前端只能"隐藏后看不见"无法回看
- 复用 S5 的 ConfirmDialog 模式

### B. 离开工作区（消费 #29）

- 前提：后端 `DELETE /workspaces/{ws}/members/{user_id}` 放宽为允许 self-removal
- Settings 危险区出 "离开工作区" 按钮（viewer/editor 默认显示，owner 仅在 owner 数 > 1 时显示）
- ConfirmDialog 确认后 redirect 到 `/onboarding` 或 `/`

### C. 工作区软归档（等后端）

- 前提：后端 `POST /workspaces/{wsId}/archive` ready
- Settings 危险区从 disabled 占位换真按钮
- 强 ConfirmDialog —— 要不要 GitHub 删 repo 那种"输入 ws name 确认"？brainstorm 时讨论
- 归档后路径：sidebar 隐藏？还是显示 + 灰色 + "已归档" chip？

### D. 文件预览 / 缩略图

- S5 上传后 asset 列表只有文件名 + 大小，点不开
- 图片：内联缩略图 (60x60) + 点击放大 modal
- PDF：第一页缩略图（需要 pdf.js）+ "在新窗口打开"
- 其他：图标 + 下载链接
- 工程量大，可能要拆 S6.0 (图片) + S6.1 (PDF)

### E. 真实 LLM call 联通（后端 mock 不够用了）

- 整个 S5 测试期间 daemon 没启过，agent 永远不回复
- S6 起码要让 brainstorm 时能跑通"发消息 → agent 真回复"一次完整流程
- 这其实是后端 + daemon 的活，不是纯前端 —— 但前端要确认 ThinkingBar 接 WS `run.completed` 事件正确清理（目前是 5s polling 兜底）
- 把 `useActiveRuns` 的 `refetchInterval` 撤掉，改成 WS 事件 invalidate runs query

### F. Cmd+K 全局搜索

- 跨 ws / project / task / agent / asset 搜索
- 后端需要 search endpoint —— 可能立新 gap
- 工程量中等，影响面广

### I. 拖拽 + 粘贴上传

- S5 现在是 button + multi-select picker
- 加 drag-over 高亮 + Cmd+V 截图粘贴
- 工程量小，体验好

### J. #30 移除 base64 兜底（前端清理）

- 等后端 WS publish 路径过 `MessageView` 后
- 删 `parseMessageContent` 里的 `coerceContent` + `enrich-message.ts` 里的 base64 metadata 兜底
- 纯清理，1 hour

### K. S5 实测发现但没立 gap 的小问题（brainstorm 时翻一遍）

- Composer 序列化 mentions 时 `@coder 你好` 变成 `@coder  你好`（双空格）
- ConfirmDialog 各处使用的 UX 一致性 review
- AddMemberModal "复制注册链接" 按钮的 1.5s "已复制"反馈太快，肉眼可能看不到

### L. Evaluations / Telemetry?

- 目前完全没 logging / metrics / 错误上报
- S6 范围内不一定该做，但 brainstorm 时讨论一下"运维上线最低门槛"

## S6 不在范围（明确排除）

- **深色主题**（不做）
- **移动端响应式**（不做，desktop-only 维持）
- WS owner 移交（永不或 S7+）
- 评论 / @人 在 artifact / asset 上（未来）
- 版本管理 / artifact 历史（未来）
- 真实付费 / billing
- i18n（除中文外其他语言）

## 关键文档（按重要度）

S6 brainstorm 优先读：

1. `D:\brainrot_frontend\docs\BACKEND_GAPS.md` —— 重点看 #28/#29/#30 + 末尾的 ws 归档 follow-up
2. `D:\brainrot_frontend\docs\API.md` —— 看 §"列出任务产出"（artifact 接口现状）、§"鉴权矩阵"
3. `D:\brainrot_frontend\docs\FRONTEND.md` M7+ 段
4. `D:\brainrot_frontend\docs\superpowers\specs\2026-05-19-s5-collab-and-uploads-design.md` §"Not in scope" —— S6 待办原始依据
5. `D:\brainrot_frontend\docs\superpowers\plans\2026-05-19-s5-collab-and-uploads.md` —— 看 S5 plan 末尾 DoD + 验收清单
6. PR #8 review comments（如果合并前有 review 留言）
7. `D:\brainrot_frontend\hooks\useActiveRuns.ts` + `D:\brainrot_frontend\components\task-detail\ThinkingBar.tsx` —— 看 S5 末尾接 runs endpoint 的兜底实现，#30 / E 项时需要回归
8. `D:\brainrot_frontend\lib\parse-message.ts` + `D:\brainrot_frontend\lib\chat\enrich-message.ts` —— base64 兜底逻辑，J 项时要删

## 关键决策项（brainstorm 必问）

### α. S6 范围裁剪

- 选项 1：A+B+J 三项小收尾（最务实，1-2 周）—— artifact 排除 + 自离 + #30 清理。功能向前推进，工程量可控。
- 选项 2：完整体验提升包（A+B+D+I，2-3 周）—— 加文件预览 + 拖拽粘贴。让产品"看着像 v1"。
- 选项 3：基础设施补强（A+B+E+J，2 周）—— 加真实 LLM call 联通 + ThinkingBar WS 优化。让用户能跑 end-to-end demo。
- 选项 4：A+B+I+J 体验小步快走（D 推到 S7）—— 拖拽粘贴上传立刻有体感，文件预览拆出来单独立项

### β. 后端依赖排期

- 后端 #28/#29 / ws 归档什么时候 ready？决定了 A/B/C 能不能进 S6
- 如果后端这一阵忙别的，S6 应该跑无后端依赖的部分（D/F/I/J/K）
- brainstorm 时先和后端沟通

### γ. 真实 LLM call (E) 的归属

- 是 S6（前端 WS 接事件优化）还是 S6.x（后端 + daemon 联调）？
- 没有真实 LLM call 整个产品永远是"看上去能用"。决定优先级。

### δ. 文件预览深度 (D)

- 选项 1：只图片缩略图 + 点开 modal（图片占 80% 上传，覆盖最广）
- 选项 2：图片 + PDF 第一页 thumbnail（pdf.js 加载成本 + 字体）
- 选项 3：全部类型都开（视频、音频、Office docs...）—— 超 S6 范围

### ε. Cmd+K (F)

- 后端需要新搜索接口（fulltext？trigram？）—— 立新 gap
- 前端 UI：modal + 列表（参考 GitHub Cmd+K / Linear）
- 索引哪些资源：tasks / projects / agents / asset filenames / message text？
- 工程量取决于后端，brainstorm 时讨论

## 产出

第一步：用 `superpowers:brainstorming` skill 启动 S6 brainstorm。沿用 S5 流程：一题一题问 a/b/c 让我选，最后产出 spec → plan → 审 spec → 批准 → subagent-driven 执行 → QA。

执行前提：

1. 跟后端 sync #28/#29/#30/ws-archive 排期
2. 后端 rebuild + 起服务（`cd D:\brainrot && git pull && go build -o bin/server.exe ./cmd/server && bin\server.exe`）
3. **重要：subagent 执行任何 UI 任务前都必须 `npm run dev` + 浏览器点一遍**（S5 教训：所有 task 都跑了 tsc/lint/build 但没人开浏览器，#24 收尾的 messages 解码漏洞和 ThinkingBar 假阳性都是用户手测时才发现。S6 explicitly 加这条到每个 UI task 的 acceptance criteria）

启动方式：用 superpowers brainstorming skill 启动 S6 子项目，第一题先问决策 α（S6 范围）。
