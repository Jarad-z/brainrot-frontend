// ui_design/chat/parts/ToolResult.jsx
// Placeholder — current rendering happens inside ToolUse. Task 12 will
// promote this to a real standalone component.

function ToolResult({ msg }) {
  return null;
}

window.ToolResult = ToolResult;
